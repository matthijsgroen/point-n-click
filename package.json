{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "nx run-many --target=build",
    "watch": "nx run-many --target=watch --parallel=7"
  },
  "devDependencies": {
    "nx": "15.0.1"
  },
  "resolutions": {
    "typescript": "~4.7.3"
  }
}
