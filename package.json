{
  "name": "point-n-click-repo",
  "packageManager": "yarn@3.5.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "nx run-many --target=build",
    "watch": "nx run-many --target=watch --parallel=7",
    "test": "jest"
  },
  "devDependencies": {
    "@parcel/transformer-typescript-types": "2.8.3",
    "jest": "^29.5.0",
    "nx": "15.0.1",
    "ts-jest": "^29.0.5",
    "typescript": ">=3.0.0"
  },
  "resolutions": {
    "typescript": "~4.7.3"
  },
  "jest": {
    "preset": "ts-jest"
  }
}
